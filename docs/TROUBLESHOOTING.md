# Troubleshooting

## Common Issues
- **DB connection errors**: Check `.env` and `DATABASE_URL`.
- **LTREE errors**: Ensure `CREATE EXTENSION ltree` in PostgreSQL.
- **403 Forbidden**: Check role permissions and scope path.
